platform :ios, '10.0'
use_frameworks!

abstract_target 'Shared' do
  pod 'Freddy', git: 'https://github.com/bignerdranch/Freddy.git', tag: '3.0.2'
  pod 'Moya/ReactiveSwift', '~> 8.0.3'
  pod 'ReactiveSwift', '~> 1.1.1'
  pod 'SAMKeychain', '~> 1.5.2'

  target 'Tootbot' do
  end

  target 'TootClient' do
  end

  target 'TootModel' do
  end
end
